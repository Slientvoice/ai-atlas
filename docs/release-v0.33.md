# v0.33 发布验证记录

验证日期：2026-09-19。范围：首页定稿、统一导航、索引地图存档移除、维护文档。

## 自动检查

- TypeScript 类型检查通过。
- 7 项导航历史测试通过：目录首页别名、锚点、直接进入、双向历史、跨站隔离、未知记录及无 Navigation API 的降级路径。
- 8 个发布页面、224 项静态站内链接与资源检查通过，目标文件及静态锚点存在，无站内 `_blank`。
- 导航同步脚本重复执行结果一致，无重复导航。
- Vite 生产构建与 `git diff --check` 通过。

## 浏览器验证

在桌面 1280×720 与手机 390×844 视口验证：

- 直接打开索引地图时，后退/前进禁用，返回首页正常。
- 首页 → 工业现场 → 蓝图屋顶线模块；后退返回工业现场，前进恢复蓝图，刷新后按钮状态正确。
- 蓝图深链接对应模块已展开，标题停靠在导航下方；初始化不再将页面带离目标。
- 目录可用键盘展开，Escape 收起并归还焦点；菜单各内容页可达。
- 手机导航分两行，后退、前进与首页操作均有 44px 点击高度；目录正常展开。
- 首页模型剖面可切换步骤，系统关系图与三个入口保留；浅色模块动效继续运行。
- 索引地图旧旗舰候选存档已移除；进度页显示 v0.33，修复旧时间线长单词造成的窄屏溢出。
- 已查看的测试页面无控制台错误。旧浏览器降级由单元测试覆盖，本次未做多浏览器全量兼容性认证。

技术活页的事实审校与 45 个实验的算法回归不属于此次导航发布范围。
