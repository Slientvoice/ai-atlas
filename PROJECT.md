# 项目维护记录

更新：2026-09-19 · v0.33。

## 定位与内容

技术是主线，法律与合规是沿技术环节补充的观察视角。首页先提供整体位置与清晰入口，再引导读者进入具体机制和实验。不要把尚未建成的专题展示为已可访问页面。

工程蓝图、工业现场、模型谱系是三个主要入口。索引地图供按词查找，入门旅程供初学者顺序阅读，进度页记录建设与核验状态。技术活页继续保留各自盘点日期与版本，门户升级不表示技术资料重新核验。

## 源码与发布物

| 路径 | 维护方式 |
| --- | --- |
| `src/portal/page.tsx`、`home.css` | 首页内容、布局与视觉 |
| `src/portal/OverviewMap.tsx`、`ModelCutaway.tsx` | 首页系统关系图与模型剖面 |
| `src/portal/AmbientEffects.tsx` | 既有流体背景与字符动效 |
| `src/portal/SiteNavigation.tsx` | 首页对共享导航的 React 接入 |
| `assets/site-navigation.mjs`、`.css`、`.d.mts` | 导航目录、行为、样式与类型声明的唯一来源 |
| `blueprint.html` 等内容页 | 直接维护正文；导航标记块由脚本生成 |
| `portal.html`、`src/main.tsx`、`vite.config.ts` | 首页构建入口与配置 |
| `index.html`、`assets/portal-*.js/css` | 构建产物，不直接改压缩代码 |
| `scripts/`、`tests/` | 导航同步、产物复制、链接检查与历史边界测试 |

v0.33 将此前独立预览工程中的首页源码迁入本仓库。此后以 `src/portal/` 为维护源，不再依赖个人电脑上的外部预览目录。`font-fitting.html` 为保留的字体开发样例，有统一返回入口，但不列入读者目录。

## 视觉与交互

首页保留 Atlas Flow 的冷白、蓝紫流体背景与玻璃模块。全局仅一套氛围背景；字符交互仅用于开场和阅读路线两个浅色模块，不覆盖导航、密集图示与暗色方法区。移动端与减少动态设置延续降级逻辑。

所有主入口都有明确中文动作文字和足够的点击面积。跨页行为遵守 [导航规范](docs/navigation.md)，不要逐页另写一套返回逻辑。

## 本次决策与后续事项

- 门户先展示系统概览，三个主入口采用整卡可点击入口。
- 模型剖面采用原创 SVG，展示分词、向量、注意力、前馈、预测和自回归循环，明确仅为示意。
- 法律与合规列为后续建设方向；暂不增加虚假的专题入口。
- 索引地图删除旧的“规划存档 · 当年的旗舰图候选”整节，保留七层 28 节点与规则手册。
- 全站导航、站内跳转和深链接一起统一；首页始终可达。
- 后续内容审校与专题建设仍按进度页排期，本次不替代技术或法律实质核验。
